<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:tx="http://www.springframework.org/schema/tx" xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
           http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-3.1.xsd
           http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.1.xsd">

    <!--Define GetUnsentMessage class-->
    <bean id="getUnsentMessageJob" class="com.worthsoln.job.GetUnsentMessageJob" />

    <!--Define SendMailJob class-->
    <bean id="sendMailJob" class="com.worthsoln.job.SendMailJob" />

    <!--Define SendMailJob's method configuration-->
    <bean id="getUnsentMessageJobTask" class="org.springframework.scheduling.quartz.MethodInvokingJobDetailFactoryBean">
        <property name="targetObject">
            <ref bean="getUnsentMessageJob" />
        </property>
        <property name="targetMethod">
            <value>execute</value>
        </property>
    </bean>

    <!--Define SendMailJob's method configuration-->
    <bean id="sendMailJobTask" class="org.springframework.scheduling.quartz.MethodInvokingJobDetailFactoryBean">
        <property name="targetObject">
            <ref bean="sendMailJob" />
        </property>
        <property name="targetMethod">
            <value>execute</value>
        </property>
    </bean>

    <!--triggers -->
    <bean id="getUnsentMessageJobProcessDoTime" class="org.springframework.scheduling.quartz.CronTriggerBean">
        <property name="jobDetail" ref="getUnsentMessageJobTask" />
        <property name="cronExpression" value="${quartz.getUnsentMsg.cron}" />
    </bean>

    <!--triggers -->
    <bean id="sendMailProcessDoTime" class="org.springframework.scheduling.quartz.CronTriggerBean">
        <property name="jobDetail" ref="sendMailJobTask" />
        <property name="cronExpression" value="${quartz.sendMail.cron}" />
    </bean>

    <bean id="startQuartz" lazy-init="false" autowire="no" class="org.springframework.scheduling.quartz.SchedulerFactoryBean">
        <property name="triggers">
            <list>
                <ref bean="getUnsentMessageJobProcessDoTime" />
                <ref bean="sendMailProcessDoTime" />
            </list>
        </property>
        <property name="taskExecutor" ref="executor" />
    </bean>

    <bean id="executor" class="org.springframework.scheduling.concurrent.ThreadPoolTaskExecutor">
        <property name="corePoolSize" value="${quartz.corePool.size}" />
        <property name="maxPoolSize" value="${quartz.maxPool.size}" />
        <property name="queueCapacity" value="${quartz.queue.capacity}" />
    </bean>
</beans>